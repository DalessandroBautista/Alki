import { StyleSheet } from 'react-native';

export const homeStyles = StyleSheet.create({
  safeContainer: {
    flex: 1,
    backgroundColor: '#f5f8fa',
  },
  container: {
    flex: 1,
  },
  contentContainer: {
    paddingBottom: 80, // Aumentado para asegurar espacio al final
  }
});